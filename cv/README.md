# Curriculum vitae

The editable source is `cv.typ`; the public PDF is generated at `dist/cv.pdf`.

Compile from the repository root with:

```sh
npm run build:cv
```

The document is built with Typst 0.15.1. Keep the generated PDF committed so the homepage always has a stable `/cv.pdf` URL.
